{
  "name": "kuran-rehberi",
  "version": "1.8.0",
  "description": "Kuran Arastirma Platformu",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "node start.js",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "engines": {
    "node": ">=18.0.0"
  }
}